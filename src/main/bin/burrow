#! /bin/zsh

SCRIPT_DIR=$(dirname "$0")
JAR_NAME="burrow-1.0.0-all.jar"
BURROW_CLIENT_CLASS="me.jameschan.burrow.BurrowClient"
cd "$SCRIPT_DIR/../../../build/libs" || exit
java -cp "$JAR_NAME" "$BURROW_CLIENT_CLASS" "$@"